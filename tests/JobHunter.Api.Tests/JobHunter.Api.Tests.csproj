<Project Sdk="Microsoft.NET.Sdk.Web">

  <!--
    API host suites (F9). Unit tests drive the scope-plus-Owner authorisation predicate directly; the
    WebApplicationFactory-backed tests assert the host's fallback-deny, RFC 7807 shape and anonymous
    surface with zero network — the API composes over stubbed Application/Infrastructure ports.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\JobHunter.Api\JobHunter.Api.csproj" />
    <ProjectReference Include="..\JobHunter.TestKit\JobHunter.TestKit.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
  </ItemGroup>

</Project>
