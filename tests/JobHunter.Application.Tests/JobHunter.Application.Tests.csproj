<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Unit tests for the Application-layer platform primitives (T11, T16): the telemetry instruments and
    label allowlist, the correlation scope, and the dead-letter replay contract types.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\JobHunter.Application\JobHunter.Application.csproj" />
    <ProjectReference Include="..\JobHunter.TestKit\JobHunter.TestKit.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The labelled dedup corpus (T06) and the frozen fingerprint set are data files read at test time. -->
    <PackageReference Include="YamlDotNet" />
    <None Update="Data\dedup-corpus.yaml" CopyToOutputDirectory="PreserveNewest" />
    <None Update="Data\fingerprints.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
